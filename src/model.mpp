export module model;

import std;

import rewriterule;
import runner;

export
struct Model {
  // std::string title;
  std::string symbols;
  RewriteRule::Unions unions;
  bool origin;
  NodeRunner program;
  bool halted = false;
};
