export module consoleapp;

import std;

import grid;

import ftxui;

import model;
import parser;
import render;

export
struct ConsoleApp {
  auto operator()(std::span<const std::string_view> args) noexcept -> int;
};
